% Exc 1 

clear; clc; 


%% a) Write a short program to generate N = 10000 points

N = 10000; 
Y = rand(10000,1);
X = zeros(size(Y));
a = Y<0.5;
b = Y >= 0.5; 
X(a) = -1+sqrt(2*);

% histogram(X,50,'Normalization','pdf')
