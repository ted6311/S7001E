
clear; clc; 

N = 10000; 
 A = 12;
x = rand(N, A);
x1 = zeros(N,1);
for i = 1:N 
    for j = 1:A
        x1(i) = x1(i) + x(i,j); 

    end
    x1(i) = x1(i)-A/2;
end


histogram(x1, 200, 'BinWidth', 0.1, 'BinLimits', [-10, 10])